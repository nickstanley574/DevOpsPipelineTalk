jobs:
  include:
    - stage: "Build"
      script: ./build.sh 
      if: branch = develop
    - stage: "Test"
      if: branch =~ /(feature\/.*|ops\/.*|develop)/
      script: ./tests.sh
    - stage: "Deploy"
      if: branch = develop
      script: "./Deploy.sh"